<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<AnchorPane prefHeight="300.0" prefWidth="400.0" xmlns="http://javafx.com/javafx/17.0.12" xmlns:fx="http://javafx.com/fxml/1" fx:controller="mx.edu.utez.ejercicioexamen.controllers.RecuperarContraCodigoController">

    <VBox alignment="CENTER" spacing="15.0" AnchorPane.leftAnchor="40.0" AnchorPane.rightAnchor="40.0" AnchorPane.topAnchor="40.0">

        <Label style="-fx-font-size: 20px; -fx-font-weight: bold;" text="Recuperar contraseña" />

        <TextField fx:id="correoField" maxWidth="300" promptText="Ingresa el código que llegó a tu correo electrónico" />

        <Button fx:id="solicitarButton" maxWidth="300" onAction="#handleCambio" text="Confirmar" />
      <VBox maxWidth="300.0" prefWidth="300.0" spacing="10.0" visible="false" VBox.vgrow="ALWAYS">
         <children>
      
              <HBox maxWidth="300.0">
                  <StackPane prefWidth="270.0">
                      <TextField fx:id="visiblePasswordField" managed="false" maxWidth="300" promptText="Nueva Contraseña" visible="false" />
                      <PasswordField fx:id="passwordField" maxWidth="300" promptText="Nueva Contraseña" />
                  </StackPane>
                  <Button fx:id="togglePasswordButton" onAction="#togglePasswordVisibility" text="👁" />
              </HBox>
      
              <HBox maxWidth="300.0">
                  <StackPane prefWidth="270.0">
                      <TextField fx:id="repeatVisiblePasswordField" managed="false" maxWidth="300" promptText="Repite la Nueva Contraseña" visible="false" />
                      <PasswordField fx:id="repeatPasswordField" maxWidth="300" promptText="Repite la Nueva Contraseña" />
                  </StackPane>
                  <Button fx:id="repeatTogglePasswordButton" onAction="#repeatTogglePasswordVisibility" text="👁" />
              </HBox>
            <Button maxWidth="300.0" mnemonicParsing="false" text="Cambiar contraseña" />
         </children>
      </VBox>

        <Label fx:id="errorLabel" textFill="red" visible="false" />

    </VBox>
</AnchorPane>
